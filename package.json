{
  "name": "super-public-flow",
  "version": "1.0.0",
  "description": "tools to pull super public data, send to mongodb, convert to csv",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sra",
  "license": "ISC",
  "dependencies": {
    "assert": "^1.3.0",
    "fs": "0.0.2",
    "moment": "^2.10.6",
    "mongodb": "^2.0.49"
  }
}
